/** Compact header chrome (solo theme toggle, avatar-only profile) below desktop width. */
export const APP_SHELL_COMPACT_CHROME_QUERY = '(max-width: 80rem)';

/** Fixed mobile toolbar: hide header search, show Find jobs shortcut. */
export const APP_SHELL_MOBILE_TOOLBAR_QUERY = '(max-width: 48rem)';
